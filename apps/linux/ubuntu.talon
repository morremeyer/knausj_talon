os: linux
-

action(user.desktop_down):
    key(ctrl-alt-down)

action(user.desktop_up):
    key(ctrl-alt-up)

action(user.move_window_desktop_down):
    key(ctrl-alt-shift-down)

action(user.move_window_desktop_up):
    key(ctrl-alt-shift-up)
